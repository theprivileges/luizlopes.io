services:
  jekyll_serve:
    image: bretfisher/jekyll-serve
    environment:
      - LANG=en_US.UTF-8
      - LANGUAGE=en_US.UTF-8
      - LC_ALL=en_US.UTF-8
    volumes:
      - '.:/site'
    ports:
      - 4000:4000
